resources:
  pipelines:
    invoice_pipeline:
      name: invoice_pipeline
      configuration:
        "catalog_name": "reggie_pierce"
        "schema_name": "invoice"
        "volume_name": "documents"
        "volume_path": ""
      libraries:
        - glob:
            include: /Workspace/Users/reggie.pierce@databricks.com/invoice_pipeline/transformations/**
      catalog: reggie_pierce
      channel: CURRENT
      environment:
        dependencies:
          - ' git+https://github.com/reggie-db/reggie-bricks-py.git#subdirectory=reggie-tools'
          - magika
          - resvg_py
          - pypdf
      photon: true
      root_path: /Workspace/Users/reggie.pierce@databricks.com/invoice_pipeline
      schema: invoice
      serverless: true
